{
  "name": "architecture",
  "version": "1.0.0",
  "scripts": {
    "build": "shx rm -rf ../docs && asciidoctor -D ../docs -a imagesdir=./images -r asciidoctor-diagram index.adoc && shx cp -R images ../docs"
  },
  "dependencies": {
    "shx": "^0.4.0"
  }
}
